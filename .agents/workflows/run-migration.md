---
description: Run a Supabase database migration
---

// turbo-all
1. Run the migration TypeScript file using tsx:
```
npx tsx supabase/migrations/<migration-file>.ts
```

Replace `<migration-file>` with the actual filename (e.g. `20260323_initial_schema.ts`).
